import { styled } from 'styletron-react'
import styles from './styles'

export const BaseButton = styled('a', styles.baseButton)
export const BaseButtonContent = styled('div', styles.baseButtonContent)
export const BaseButtonText = styled('div', styles.baseButtonText)
export const BaseButtonIcon = styled('svg', styles.baseButtonIcon)
